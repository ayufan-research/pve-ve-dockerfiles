diff --git a/debian/control b/debian/control
--- a/debian/control
+++ b/debian/control
@@ -12,9 +12,6 @@ Provides: proxmox-virtual-environment,
 Depends: apt,
          openssh-client,
          openssh-server,
-         proxmox-archive-keyring,
-         proxmox-default-kernel,
-         proxmox-kernel-helper (>= 9.0~),
          pve-manager (>= 9.0~),
          pve-qemu-kvm,
          qemu-server (>= 9.0~),
