ARG ARCH=
FROM ${ARCH}debian:bookworm AS toolchain

FROM toolchain as run_env
RUN apt-get -y update
ADD /dockerfiles/run /scripts/
RUN /scripts/install.bash /release proxmox-ve proxmox-backup-restore-image proxmox-firewall
